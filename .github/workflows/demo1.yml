name: hello-world
on:
  push:
    branches:
      - main
    paths:
      - 'doc/**'
  schedule:
    - cron:  '*/5 * * * *'
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!!!!"
